# Loop through all files ending with rc in ~/.config/direnv and source them
for rcfile in "$HOME/.config/direnv/"*rc; do
  # Check if the file actually exists (to handle the case where no files match)
  if [[ -f "$rcfile" ]]; then
    source "$rcfile"
  fi
done
